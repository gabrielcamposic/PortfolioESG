# --- Optimized Portfolio Script Parameters for PortfolioESG ---
# Script C_OptimizedPortfolio.py: combines ideal portfolio (A) with holdings (B)

# --- Files and paths ---
OPTIMIZED_RESULTS_FILE = ~/PortfolioESG/data/results/optimized_portfolio_history.jsonl
OPTIMIZED_LATEST_JSON = ~/PortfolioESG/data/results/optimized_recommendation.json
OPTIMIZED_LOG_FILE = ~/PortfolioESG/logs/optimized.log

# --- Score Weights (must sum to 1.0) ---
# Pesos para score do portfolio otimizado
WEIGHT_EXPECTED_RETURN = 0.4
WEIGHT_SHARPE_RATIO = 0.4
WEIGHT_MOMENTUM = 0.2

# --- Threshold mínimo de retorno excedente (%) ---
# Se o retorno excedente do portfolio otimizado vs holdings for menor que isso,
# a recomendação será "manter holdings"
MIN_EXCESS_RETURN_THRESHOLD = 0.5

# --- Transaction Cost Calculation ---
# DYNAMIC = calcula baseado no histórico de transações (recomendado para custos reais)
# FIXED = usa valor fixo definido abaixo
TRANSACTION_COST_MODE = DYNAMIC

# Para cálculo dinâmico: número mínimo de transações ou meses
TRANSACTION_COST_MIN_TRANSACTIONS = 20
TRANSACTION_COST_MIN_MONTHS = 6

# Custo fixo de transação (% por trade) - usado se TRANSACTION_COST_MODE = FIXED
# Seu custo real da Ágora é ~0.03%, muito abaixo da média do mercado
TRANSACTION_COST_FIXED_PCT = 0.03

# --- Expected Return Window ---
# Janela temporal para cálculo de expected return (dias úteis)
EXPECTED_RETURN_WINDOW_DAYS = 252

# --- Optimization Settings ---
# Número de portfolios candidatos a gerar entre holdings e ideal
NUM_CANDIDATE_PORTFOLIOS = 100

# Mínimo de overlap (%) entre holdings e recomendação para considerar transição
MIN_OVERLAP_THRESHOLD = 0.0

# Máximo de transações permitidas em uma recomendação (0 = sem limite)
MAX_TRANSACTIONS = 0

# --- Target Quality Diagnostics ---
# Used only for diagnostics/shadow fields at this stage.
TARGET_EXTREME_UPSIDE_PCT = 150
TARGET_REJECT_UPSIDE_PCT = 300
TARGET_LOW_PRICE = 1.00
TARGET_STALE_DAYS = 45
TARGET_MAX_FALLBACK_QUALITY = 0.35
TARGET_LOW_LIQUIDITY_AVG_VOLUME = 100000
TARGET_CLASS_TARGET_TOLERANCE_PCT = 15
TARGET_CLASS_PRICE_RATIO = 3
TARGET_DISTRESSED_RETURN_PCT = -50

# --- Adjusted Expected Return Diagnostics ---
# Shadow return only; official recommendation still uses the raw return.
RETURN_ADJUSTMENT_CAP_PCT = 150
RETURN_ADJUSTMENT_FLOOR_PCT = -80
RETURN_ADJUSTMENT_BASE_PCT = 0
RETURN_ADJUSTMENT_REJECT_BASE_PCT = 0
RETURN_ADJUSTMENT_UNCERTAINTY_PENALTY_PCT = 0

# --- Market Regime Diagnostics ---
# Diagnostic only; future phases may use these fields in shadow decision gates.
REGIME_BENCHMARK_TICKER = ^BVSP
REGIME_LOOKBACK_3M_DAYS = 63
REGIME_LOOKBACK_6M_DAYS = 126
REGIME_DRAWDOWN_STRESS_PCT = 10
REGIME_NEGATIVE_BREADTH_PCT = 60
REGIME_DRAWDOWN_BREADTH_PCT = 45
REGIME_ASSET_DRAWDOWN_THRESHOLD_PCT = 20
REGIME_VOLATILITY_WATCH_PCT = 25
REGIME_DISPERSION_WATCH_PCT = 35

# --- Shadow Rebalance Gate ---
# Diagnostic only; compares official decision against a stricter trade-readiness gate.
SHADOW_BASE_HURDLE_PCT = 0.5
SHADOW_SLIPPAGE_ESTIMATE_PCT = 0.15
SHADOW_TAX_DRAG_ESTIMATE_PCT = 0
SHADOW_MODEL_UNCERTAINTY_PENALTY_PCT = 0.5
SHADOW_MIN_PERSISTENCE_DAYS = 2
SHADOW_TURNOVER_BUDGET_PCT = 35
SHADOW_CONFIDENCE_FLOOR = 0.60
SHADOW_MAX_SUSPICIOUS_RETURN_CONTRIBUTION_PCT = 35
SHADOW_PARTIAL_REBALANCE_MIN_GAIN_PCT = 1.0

# --- Shadow Execution States ---
# Diagnostic only; translates the shadow decision into today's executable action.
EXECUTION_ASSET_TOLERANCE_BAND_PCT = 2.0
EXECUTION_SECTOR_TOLERANCE_BAND_PCT = 5.0
EXECUTION_WEEKLY_TURNOVER_BUDGET_PCT = 12
EXECUTION_MONTHLY_TURNOVER_BUDGET_PCT = 35
EXECUTION_MIN_TRADE_VALUE_BRL = 25
EXECUTION_MAX_ACTIONS = 6

# --- Stable Optimization Shadow ---
# Diagnostic only; selects a turnover-aware stable candidate in parallel.
TURNOVER_PENALTY_LAMBDA = 0.05
STABLE_TURNOVER_TARGET_PCT = 12
STABLE_TURNOVER_EXCESS_PENALTY_LAMBDA = 0.10
STABLE_UNCERTAINTY_PENALTY_LAMBDA = 0.03
STABLE_CONCENTRATION_PENALTY_LAMBDA = 0.02
STABLE_SUSPICIOUS_RETURN_PENALTY_LAMBDA = 0.03

# --- Independent Balanced Target Portfolio ---
# Builds the investable Balanced portfolio before execution/turnover decisions.
BALANCED_MAX_ASSET_WEIGHT_PCT = 12
BALANCED_MAX_SECTOR_WEIGHT_PCT = 30
BALANCED_MAX_LOW_QUALITY_WEIGHT_PCT = 8
BALANCED_MIN_POSITIONS = 9
BALANCED_MAX_POSITIONS = 12
BALANCED_MAX_HHI = 0.13
BALANCED_MAX_TOP5_WEIGHT_PCT = 60
BALANCED_DISCRETIZATION_TOLERANCE_PCT = 1.25

# Liquidity filters: require traded value above the floor and cap target size
# relative to average daily traded value.
MIN_BALANCED_ADTV_BRL = 1000000
MAX_BALANCED_POSITION_TO_ADTV_RATIO = 0.05
BALANCED_MIN_PRICE_HISTORY_DAYS = 126
BALANCED_MIN_TARGET_QUALITY_SCORE = 0.20
BALANCED_EXCLUDE_CLASS_MISMATCH = 1

# Expected return blend for the Balanced portfolio.
BALANCED_TARGET_RETURN_WEIGHT = 0.30
BALANCED_HISTORICAL_RETURN_WEIGHT = 0.25
BALANCED_MOMENTUM_WEIGHT = 0.15
BALANCED_VALUATION_WEIGHT = 0.15
BALANCED_DIVIDEND_WEIGHT = 0.10
BALANCED_TARGET_RETURN_CAP_PCT = 60
BALANCED_COMPONENT_CAP_PCT = 60
BALANCED_DIVIDEND_CAP_PCT = 15

# Balanced score adjustments.
BALANCED_QUALITY_BONUS_PCT = 3
BALANCED_COMPOSITE_SCORE_BONUS_PCT = 3
BALANCED_UNCERTAINTY_PENALTY_PCT = 6
BALANCED_VOLATILITY_PENALTY_LAMBDA = 0.08
BALANCED_DRAWDOWN_FREE_PCT = 15
BALANCED_DRAWDOWN_PENALTY_LAMBDA = 0.04
BALANCED_LOW_QUALITY_PENALTY_PCT = 8
BALANCED_LIQUIDITY_PENALTY_PCT = 2
BALANCED_REGIME_STRESS_PENALTY_PCT = 2
BALANCED_STRESS_TARGET_WEIGHT_MULTIPLIER = 0.50
BALANCED_SCORE_WEIGHT_POWER = 1.20
